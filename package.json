{
  "scripts": {
    "clean": "rm -rf .cache dist",
    "serve": "parcel src/**/*.html",
    "start": "yarn clean && yarn serve"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.2"
  }
}
